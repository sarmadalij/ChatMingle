<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatMingle" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.ChatMingle" parent="Base.Theme.ChatMingle" >
        <!--    we can change color of status bar -->
        <item name="android:statusBarColor">
            @color/arrowbody
        </item>
    </style>

    <!--    for toolbar custom style - background and text color-->
    <style name="ThemeOverlay.MyPopupTheme" parent="Theme.Material3.DayNight">
        <item name="android:textColor">@color/white</item>
        <item name="popupMenuBackground">@drawable/popupmenu</item>
    </style>

</resources>